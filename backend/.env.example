# ============================================================================
# AI Study Assistant - Backend Environment Variables
# ============================================================================

# PRODUCTION (Railway/Render/Fly.io) KULLANIMI:
# 1. Platform dashboard'da environment variables ekleyin
# 2. Hassas bilgileri (API keys) mutlaka güvenli şekilde saklayın
# 3. Deploy edin

# LOCAL DEVELOPMENT KULLANIMI:
# 1. Bu dosyayı .env olarak kopyalayın: cp .env.example .env
# 2. Değerleri doldurun (özellikle OPENAI_API_KEY)
# 3. python main.py

# ============================================================================
# OpenAI API Configuration (ZORUNLU)
# ============================================================================

# OpenAI API Key (mutlaka gerekli!)
# https://platform.openai.com/api-keys adresinden alınabilir
# Örnek: sk-proj-xxxxxxxxxxxxx
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# ============================================================================
# JWT Security (ZORUNLU)
# ============================================================================

# JWT Secret Key - Kullanıcı kimlik doğrulama için
# PRODUCTION'DA MUTLAKA değiştirin!
# En az 32 karakter, rastgele olmalı
# Generate: openssl rand -hex 32
JWT_SECRET_KEY=your-secret-key-change-in-production-min-32-chars

# ============================================================================
# Database Configuration
# ============================================================================

# Database URL
# Local development: SQLite (default)
# Production: PostgreSQL (Railway/Render otomatik sağlar)
# DATABASE_URL=sqlite:///./study_assistant.db  # Local
# DATABASE_URL=postgresql://user:pass@host:5432/dbname  # Production

# Not: Railway ve Render otomatik olarak DATABASE_URL environment variable'ını ayarlar
# Manuel ayarlama gerekmez

# ============================================================================
# CORS Configuration (İsteğe Bağlı)
# ============================================================================

# Allowed origins (frontend URL'leri)
# Virgülle ayrılmış liste
# Production'da spesifik domain'leri kullanın
# Development'ta * kullanabilirsiniz

# Local development (default)
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Production (Vercel)
# CORS_ORIGINS=https://your-app.vercel.app,https://your-app-preview.vercel.app

# Allow all (sadece development için!)
# CORS_ORIGINS=*

# ============================================================================
# Stripe Configuration (İsteğe Bağlı - Premium özellikler için)
# ============================================================================

# Stripe Secret Key
# Test için: sk_test_xxx
# Production için: sk_live_xxx
# https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key

# Stripe Webhook Secret (Stripe webhook endpoint'i için)
# https://dashboard.stripe.com/webhooks
# STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Stripe Price IDs (ürün fiyatları için)
# https://dashboard.stripe.com/products
# STRIPE_PRICE_ID_MONTHLY=price_xxx
# STRIPE_PRICE_ID_YEARLY=price_xxx

# ============================================================================
# NOTLAR
# ============================================================================

# 1. OPENAI_API_KEY zorunludur! Yoksa uygulama çalışmaz.

# 2. JWT_SECRET_KEY production'da mutlaka değiştirilmeli!
#    Güvenli key oluşturma:
#    - openssl rand -hex 32
#    - python -c "import secrets; print(secrets.token_urlsafe(32))"

# 3. DATABASE_URL:
#    - Local: SQLite otomatik kullanılır (.db dosyası)
#    - Railway/Render: Otomatik PostgreSQL URL sağlanır
#    - Manual PostgreSQL: Tam connection string girin

# 4. CORS_ORIGINS:
#    - Production'da spesifik domain'leri belirtin
#    - Wildcard (*) sadece development için kullanın
#    - Virgülle ayırarak birden fazla domain ekleyebilirsiniz

# 5. Stripe:
#    - Premium subscription özelliği için gerekli
#    - Kullanmayacaksanız bu değişkenleri boş bırakabilirsiniz
#    - Test mode: sk_test_ ve pk_test_ keys
#    - Live mode: sk_live_ ve pk_live_ keys

# 6. Environment variables'ı commit etmeyin!
#    .env dosyası .gitignore'da olmalı

# 7. Production'da environment variables şu şekilde ayarlanır:
#    - Railway: Dashboard → Variables
#    - Render: Dashboard → Environment → Environment Variables
#    - Fly.io: fly secrets set KEY=VALUE
#    - Vercel: Settings → Environment Variables

# ============================================================================
# Security Checklist
# ============================================================================

# Production'a deploy etmeden önce:
# [ ] JWT_SECRET_KEY rastgele ve güçlü (32+ karakter)
# [ ] OPENAI_API_KEY doğru ve geçerli
# [ ] DATABASE_URL PostgreSQL kullanıyor (SQLite değil)
# [ ] CORS_ORIGINS spesifik domain'lere kısıtlı (* değil)
# [ ] STRIPE_SECRET_KEY live key (test değil)
# [ ] .env dosyası git'e commit edilmemiş
# [ ] Hassas bilgiler güvenli şekilde saklanıyor
